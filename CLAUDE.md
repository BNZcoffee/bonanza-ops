# 협업 규칙 & 시스템 메모 (Bonanza Ops)

## [필수 프로토콜] 매뉴얼 선제 업데이트 (2026-07-13 확정)
워크플로·UI/UX 등 **사용자가 알아야 하는 동작**이 바뀌는 변경을 할 때는,
운영팀장이 지시하기 전에 **먼저** "이 변경으로 매뉴얼의 OO 부분이 낡았으니
이렇게 바꾸겠습니다"라고 제안하고, 승인 시 코드까지 함께 반영한다.
- 대상: 앱 내 '사용 설명서'(renderManual), '신규 입사자 빠른 시작 가이드'(quickStartCard),
  각 폼의 안내 문구, 계정 등록 안내문 템플릿(app_settings.account_guide_template).

## [필수 프로토콜 2] 권한(audience)별 매뉴얼 분류 (2026-07-13 확정)
새 기능을 추가/수정할 때는 **그 기능이 어떤 권한 대상인지 먼저 분석**한다:
일반 직원(user) · 매니저(manager) · 부서담당(dept) · 총괄·부총괄(admin).
그리고 **코드를 작성하기 전에** 다음을 먼저 물어 확정한다:
"이 기능은 [권한] 전용이므로, 해당 권한 사용자의 매뉴얼 카테고리(audience=[값])에
이렇게 설명을 추가/수정할까요?"
- 매뉴얼 노출 대상(manual_sections.audience)은 앱 사이드바 권한 게이팅(renderSidebar)과 항상 일치시킨다.
- audience 등급: user < manager/dept < admin. 관리 기능 설명이 user 탭에 노출되지 않도록 한다.
- 예: 신청유형/권한/앱·알림 설정 = admin, 업무신청·스케줄·근태·보고·면담·인수인계 = manager.
- 기준: 앱을 처음 보는 신규 입사자가 봐도 완벽히 이해되도록 최신 로직 유지.

## 현재 시스템 핵심 사실 (매뉴얼 일관성 기준)
- 로그인: **이름 + 입사일 + 비밀번호**. 초기 공통 비밀번호 = **bnz1234**
  (동명이인 코드 입력 분기 폐지 — 이름+입사일 조합은 유일, 즉시 통과.)
  (이메일 로그인/비밀번호 해시 안내는 폐기됨).
- 첫 로그인: 슬랙 멤버 ID 입력 → 슬랙 DM 6자리 인증 → 새 비밀번호 직접 설정.
- 슬랙 멤버 ID 등록 = **필수(Mandatory)**. 모든 알림이 슬랙 개인 DM으로 발송됨.
- 개인정보/슬랙ID/비밀번호는 사용자가 **[내 정보]** 페이지에서 직접 수정(운영팀 요청 불필요).
- 슬랙 발송: Supabase Edge Function `slack-notify` 경유(봇 토큰은 시크릿, 프론트 미노출).
- 매뉴얼 본문: DB `manual_sections`(audience: user/manager/dept/admin). 코드 렌더 = renderManual().
- 직원 등록: 단건(openEditUser)·일괄(openBulkAddUser) 입력 항목 100% 일치 유지.
  고용형태 = 정규직/계약직/PT. 계약직·PT는 계약근로시간 필수.
  계약직: 선택 시 계약근로 8h·휴게 60분 자동 프리필(수정 가능), 퇴사일(계약 종료일) 필수.
  시급·주근무일수는 PT(시간제) 전용(계약직·정규직 숨김). '신청유형 관리' 메뉴는 isAdminGroup 전용.
  일괄표 열(12): 이름·소속매장·직위·고용형태·계약시간·휴게·주근무일수·이메일·연락처·입사일·퇴사일·파트.

## 배포 방법
- 단일 파일 SPA(index.html). 인라인 <script> 추출 후 `node --check`로 검증 → git push(main).
- SQL 변경은 항상 index.html보다 **먼저** 실행.
